import Title from "../Title";
import type { ReactElement } from "react";

/**
 * Renders the Inbox component.
 * @returns ReactElement - The Inbox skeleton.
 */
const Inbox = (): ReactElement => {
  return <Title>Inbox</Title>;
};

export default Inbox;
