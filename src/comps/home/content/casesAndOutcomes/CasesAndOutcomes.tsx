import Title from "../Title";
import type { ReactElement } from "react";

/**
 * Renders the CasesAndOutcomes component.
 * @returns The CasesAndOutcomes skeleton.
 */
const CasesAndOutcomes = (): ReactElement => {
  return <Title>CasesAndOutcomes</Title>;
};

export default CasesAndOutcomes;
