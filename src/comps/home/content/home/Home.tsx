import Title from "../Title";
import type { ReactElement } from "react";

/**
 * Renders the Home component.
 * @returns ReactElement - The Home skeleton.
 */
const Home = (): ReactElement => {
  return <Title>Home</Title>;
};

export default Home;
