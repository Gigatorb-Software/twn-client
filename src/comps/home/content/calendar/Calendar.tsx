import Title from "../Title";
import type { ReactElement } from "react";

/**
 * Renders the Calendar component.
 * @returns The Calendar skeleton.
 */
const Calendar = (): ReactElement => {
  return <Title>Calendar</Title>;
};

export default Calendar;
