import type { ReactElement } from "react";

/**
 * The `Navbar` functional component.
 * @example
 * ```tsx
 * <Navbar />
 * ```
 * @returns The `Navbar` component skeleton.
 */
const Navbar = (): ReactElement => {
  return <nav>TWN Client</nav>;
};

export default Navbar;
