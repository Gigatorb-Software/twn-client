import Layout from "../comps/layout/Layout";
import type { ReactElement } from "react";

/**
 * The `Register` functional component which is
 * a page for the `/register` route.
 * @example
 * ```tsx
 * <Register />
 * ```
 * @returns The `Register` component skeleton.
 */
const Register = (): ReactElement => {
  return <Layout>This is the registration page!</Layout>;
};

export default Register;
